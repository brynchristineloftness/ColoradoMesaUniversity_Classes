#include <iostream>
#include <string>
using namespace std;

int main () {
    string Grade
    
    string Name
    cout << "Please enter name" << endl;
    cin >> Name;
    float GradeAverage = //Average of all Grades
    cout << "Please enter Grades" << endl;
    cin >> Grade >>
}

//Grade Calculation
if ( GradeAverage >= 90.0 ) {
    cout << "You got an A" << endl;
} else if ( GradesAverage >= 80.0 ) {
    cout << "You got a B" >> endl;
} else if ( GradesAverage >= 70.0 ) {
    cout << "You got a C" >> endl;
} else if ( GradesAverage >= 60.0 ) {
    cout << "You got a D" >> endl;
} else {
    cout << "You got an F" >> endl;
}

int NumofGrades

GradeAverage = ( //sum of #s / str.length ();


//Grade Average Calculation

int Grade;
int sum;

 for (Grade=0; Grade < 100, Grade++){
    if ( GradeAverage > 90.0 ) {
        cout << "You got an A" << endl;
    } else if ( GradesAverage > 80.0 ) {
        cout << "You got a B" >> endl;
    } else if ( GradesAverage > 70.0 ) {
        cout << "You got a C" >> endl;
    } else if ( GradesAverage > 60.0 ) {
        cout << "You got a D" >> endl;
    } else {
        cout << "You got an F" >> endl;
    }
    return 0;  
 }

 int newGrade;
 int GradesAverage;
 int counter;
 int N;

 cout << "Enter the number of test scores you will be entering" << endl;
 cin >> N;
 sum = 0 
 
 for (counter = 1; counter <= N; counter++) {
     sum = sum + counter;
cout << "The sum of the first " << N << "test grades is " << sum << endl;

 }



//sum of string #s / ( str.length() )


int main () {
    int i=10;
    while (i<100) {
        cout << "Enter your grade" << endl;
        cin >> i;
        cout << "You entered " << i << endl;
    }
int j=0;
while (j <100) {
    // Your code here 
    j = j + 1
}
return 0;
}